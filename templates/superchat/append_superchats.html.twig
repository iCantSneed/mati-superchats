{%- import "superchat/_macros.html.twig" as macros -%}

<turbo-stream action="append" target="{{ macros.stream_html_id(superchats[0].stream) }}"><template>
{%- for superchat in superchats -%}
  {{- macros.superchat_message(superchat) -}}
{%- endfor -%}
</template></turbo-stream>
